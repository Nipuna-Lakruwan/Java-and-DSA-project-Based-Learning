public class Main {
  public static void main(String[] args) {
    
    // Access Modifiers
    // - public = accessible from anywhere (within the class, outside the class, within the package, outside the package) 
    // - private = accessible only within the class 
    // - protected = accessible within the class, within the package, and by subclasses 
    // - default = accessible only within the package (no keyword) 
    
  }
}
