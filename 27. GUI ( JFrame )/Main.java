

public class Main {
  public static void main(String[] args) {
    
    // JFrame frame = new JFrame(); // create a new JFrame

    // frame.setTitle("JFrame Title Goes Here"); // set the title of the frame
    // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit the application when the frame is closed
    // frame.setResizable(false); // prevent the frame from being resized
    // frame.setVisible(true); // make the frame visible
    // frame.setSize(420, 420); // set the size of the frame

    // ImageIcon icon = new ImageIcon("monkey-funny-face-vector-logo.avif"); // create an ImageIcon
    // frame.setIconImage(icon.getImage()); // set the icon of the frame

    // frame.getContentPane().setBackground(new Color(123, 50, 250)); // set the background color of the frame

    MyFrame myFrame = new MyFrame();

    
  }
}
